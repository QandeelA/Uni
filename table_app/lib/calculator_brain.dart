
class CalculatorBrain {
  int Table_number;
  int starting_limit;
  int ending_limit;
  double _bmi;
  CalculatorBrain({this.starting_limit, this.Table_number, this.ending_limit});

  /*String calculateBMI() {
    _bmi = (starting_limit * Table_number) as double ;
    return _bmi.toStringAsFixed(1);
  }

*/

}